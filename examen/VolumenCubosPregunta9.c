#include<stdio.h>
#include<math.h>

int main(){

float l1 = 7.9, l2 = 2.5, l3 = 2.0, v1, v2, v3;
v1 = pow(l1,3);
v2 = pow(l2,3);
v3 = pow(l3,3);

printf("El volumen del cubo 1 es %f \nEl volumen del cubo 2 es %f \nEl volumen del cubo 3 es %f", v1, v2, v3);

return 0 ;

}
